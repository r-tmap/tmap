# Internal tmap function that gets factor levels with NA's

Internal tmap function that gets factor levels with NA's

## Usage

``` r
get_fact_levels_na(x, o)
```

## Arguments

- x:

  vector

- o:

  options
