# Internal tmap function to create by variables (used for faceting)

Internal tmap function to create by variables (used for faceting)

## Usage

``` r
make_by_vars(dt, tmf, smeta)
```

## Arguments

- dt:

  data.table

- tmf:

  tmf object

- smeta:

  smeta object
