# Internal method that extracts meta data from shape objects

Internal method that extracts meta data from shape objects

## Usage

``` r
tmapGetShapeMeta1(shp, o)
```

## Arguments

- shp:

  the shape object

- o:

  the list of options
