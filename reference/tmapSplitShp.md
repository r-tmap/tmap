# Internal method that split shape objects

Internal method that split shape objects. So far, only used to split
stars object (from dimension to attributes)

## Usage

``` r
tmapSplitShp(shp, split_stars_dim, smeta)
```

## Arguments

- shp:

  shape

- split_stars_dim:

  name of the dimension to split (`""` to skip)
