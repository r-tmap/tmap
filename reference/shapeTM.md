# Internal tmap function to create a tmap shape

Internal tmap function to create a tmap shape

## Usage

``` r
shapeTM(shp, tmapID = NULL, bbox = NULL, ...)
```

## Arguments

- shp:

  Shape file

- tmapID:

  tmap Identifier

- bbox:

  bounding box
