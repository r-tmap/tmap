# Set the development mode

When the so-called "development mode" is enabled, helpful messages and
timings are printed in the console

## Usage

``` r
tmap_devel_mode(devel.mode)
```

## Arguments

- devel.mode:

  logical value that determines the development mode. If omitted then
  the development mode is toggled.
